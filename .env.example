DATABASE_URL="postgresql://demo:demo@localhost:5433/demo"

# Apex domain for subdomain middleware and UI links (no https://)
DOMAIN="localhost:3000"
# Legacy alias (optional): ROOT_DOMAIN="localhost:3000"

# Production example:
# DOMAIN="demo-next.mahasooq.com"

# Server (Railway sets PORT automatically)
# HOSTNAME="0.0.0.0"
# PORT=3000
