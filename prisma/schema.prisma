generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Polygon is hardcoded in the geofence API query; schema exists for migrations only.
model Geofence {
  id String @id @default(cuid())
}
